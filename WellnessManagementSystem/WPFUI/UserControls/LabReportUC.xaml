<UserControl x:Class="PhysioApplication.UserControls.LabReportUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:editing="clr-namespace:Editing"
             d:DesignHeight="300" d:DesignWidth="800">
    <UserControl.Resources>

        <Style TargetType="{x:Type editing:EditBox}" >
            <Setter Property="HorizontalAlignment" Value="Left"  />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type editing:EditBox}">
                        <TextBlock x:Name="PART_TextBlockPart"  
                     Text="{Binding Path=Value,Mode=TwoWay, RelativeSource = 
                           {RelativeSource TemplatedParent}}">
                        </TextBlock>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="myHeaderStyle" TargetType="{x:Type GridViewColumnHeader}">
            <Setter Property="Background" Value="{StaticResource ButtonOrangeBackground}"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontFamily" Value="Calibri"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style x:Key="CustomListViewStyle" TargetType="{x:Type ListView}">
            <Setter Property="GridView.ColumnHeaderContainerStyle" Value="{StaticResource myHeaderStyle}" />
            <!--<Setter Property="GridView.ColumnHeaderTemplate" Value="{StaticResource MyGridColumnHeaderTemplate}" />
            <Setter Property="ItemContainerStyle" Value="{StaticResource MyListViewItemContainerStyle}" />-->
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="363*"/>
            <ColumnDefinition Width="437*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="76*"/>
            <RowDefinition Height="342*"/>
            <RowDefinition Height="39*"/>
        </Grid.RowDefinitions>
        <ListView Grid.Row="1" x:Name="lvReports" Grid.RowSpan="1" Grid.ColumnSpan="2">

        </ListView>
        <Button Grid.Row="0" x:Name="btnAdd" Click="btnAdd_Click" Margin="0,0,120,0" Width="50" Height="50" Background="Transparent" BorderBrush="Transparent" HorizontalAlignment="Right" Grid.Column="1">
            <Image Source="..\Images\Add.png" Width="40" Height="40"/>
        </Button>
        <Button x:Name="btnDelete" Click="btnDelete_Click" Margin="0,0,60,0" Width="50" Height="50"  Background="Transparent" BorderBrush="Transparent" HorizontalAlignment="Right" Grid.Column="1">
            <Image Source="..\Images\Remove.png" Width="40" Height="40" Margin="1"></Image>
        </Button>
        <Button Grid.Row="0" x:Name="btnSave" Click="btnSave_Click" Margin="0,0,0,0" Width="50" Height="50"  Background="Transparent" BorderBrush="Transparent" HorizontalAlignment="Right" Grid.Column="1">
            <Image Source="..\Images\Save.png" Width="40" Height="40" Margin="1"></Image>
        </Button>

    </Grid>
</UserControl>
